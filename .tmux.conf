# tmux dari Nol — starter
# https://kodepas.com/kelas/tmux-dari-nol-besar
#
# Salin ke home:  cp .tmux.conf ~/.tmux.conf
# Reload:         tmux source-file ~/.tmux.conf

set -g mouse on
set -g history-limit 50000
setw -g mode-keys vi
